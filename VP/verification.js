const form = document.getElementById("verification-form");

form.addEventListener("submit", (event) => {
	event.preventDefault();

	// TODO: Add code to verify documents and show success or error message
});
